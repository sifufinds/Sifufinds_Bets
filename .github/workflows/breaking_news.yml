name: Breaking News Bot — Auto-publish every 30 min

on:
  schedule:
    # Runs every 30 minutes, 06:00–23:00 WAT (05:00–22:00 UTC)
    - cron: "*/30 5-22 * * *"
  workflow_dispatch: # Allow manual trigger from GitHub UI

permissions:
  contents: write

jobs:
  publish-breaking-news:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r agents/python/requirements.txt

      - name: Run Sports Blog Agent — Football + Sport News
        working-directory: agents/python
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          FOOTBALL_API_KEY: ${{ secrets.FOOTBALL_API_KEY }}
        run: |
          python agent_sports_blog.py --category football
          python agent_sports_blog.py --category sportnews

      - name: Run Sports Blog Agent — Rotating sport (every 2 hours)
        working-directory: agents/python
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          # Rotate through secondary sports based on hour of day
          HOUR=$(date -u +%H)
          if   [ "$HOUR" -lt 8  ]; then python agent_sports_blog.py --category basketball
          elif [ "$HOUR" -lt 10 ]; then python agent_sports_blog.py --category tennis
          elif [ "$HOUR" -lt 12 ]; then python agent_sports_blog.py --category cricket
          elif [ "$HOUR" -lt 14 ]; then python agent_sports_blog.py --category rugby
          elif [ "$HOUR" -lt 16 ]; then python agent_sports_blog.py --category boxing
          elif [ "$HOUR" -lt 18 ]; then python agent_sports_blog.py --category f1
          elif [ "$HOUR" -lt 20 ]; then python agent_sports_blog.py --category igaming
          else python agent_sports_blog.py --category basketball
          fi

      - name: Update live ticker
        working-directory: agents/python
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python agent_sports_blog.py --ticker-only

      - name: Commit and push new articles
        run: |
          git config --local user.email "newsbot@sifufinds.com"
          git config --local user.name "SifuFinds Breaking News Bot"
          git add blog/posts.json blog/posts-data.js blog/ticker.json blog/ticker-data.js
          git diff --cached --quiet || git commit -m "🔴 Breaking news: $(date -u '+%Y-%m-%d %H:%M') UTC"
          git push
